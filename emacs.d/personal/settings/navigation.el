;;; navigation.el --- Settings for navigation

;;; Commentary:

;; Settings for navigation

;;; Code:

(ido-mode                      1)
(setq ido-everywhere           t)
(setq ido-enable-flex-matching t)

;;; navigation.el ends here
