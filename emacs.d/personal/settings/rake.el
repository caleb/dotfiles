;;; rake.el --- Settings for rake

;;; Commentary:

;; Settings for rake

;;; Code:

(eval-after-load 'projectile
  '(setq rake-completion-system projectile-completion-system))

;;; rake.el ends here
