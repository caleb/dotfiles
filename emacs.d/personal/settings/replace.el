;;; replace.el --- Settings for replacing strings

;;; Commentary:

;; Settings for replacing strings

;;; Code:

;; configure re-builder
;; (require 're-builder)
(setq reb-re-syntax 'string)

;;; replace.el ends here
