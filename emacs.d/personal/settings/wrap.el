;;; wrap.el --- Sets up line wrapping

;;; Commentary:

;; Sets up line wrapping

;;; Code:

(setq personal-wrap-limit 80)

;; force text mode for the fill column indicator
(setq fci-always-use-textual-rule t)

;;; wrap.el ends here
