setopt prompt_subst

# Use a git prompt if git is in our path
if [[ -x `which git` ]]; then
  if [[ -n $(run_from_ssh) ]]; then
    PROMPT='%{$reset_color%}%n@%m:%3~$(git-prompt)%# '
  else
    PROMPT='%{$reset_color%}%3~$(git-prompt)%# '
  fi
else
  PROMPT='%{$reset_color%}%n@%m:%3~%# '
fi

