if [[ -x `which irb` ]]; then
  alias irb='irb --readline -r irb/completion'
fi

export RUBYOPT="KU rjcode"

# if gem is installed, set our RUBYOPT to include it
if [[ -x `which gem` ]]; then
  RUBYOPT="rubygems $RUBYOPT"
fi
